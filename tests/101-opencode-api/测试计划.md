# 101-opencode-api 测试计划

日期：2026-04-22

## 1. 目标

- 验证北向 API 代理转发可用
- 验证与 opencode server 核心路由的兼容行为（方法、路径、状态码）
- 验证聚合策略（aggregate/broadcast/primary/session/current）基础可运行

## 2. 范围

- `/health`
- `/global/health`
- `/project`、`/project/current`
- `/session`、`/session/:id*`
- `/provider`、`/provider/auth`
- `/config`、`/config/providers`
- `/agent`、`/command`、`/skill`
- `/x/backends`、`/x/routes`、`/x/sse`

## 3. 环境

- opencode-stack：`http://127.0.0.1:6904`
- 本地 opencode server：`http://127.0.0.1:4096`
- 测试工具：`curl`

## 4. 入口与退出条件

### 入口条件

- `npm run build` 通过
- opencode-stack 服务可启动
- 可选：本地 opencode server 已启动（用于完整代理验证）

### 退出条件

- 脚本执行完成并输出明确通过/失败统计
- 生成测试报告：`dist/test[101-opencode-api]-20260422.md`
